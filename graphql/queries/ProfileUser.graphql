query ProfileUser($id: String!) {
  user(id: $id) {
    bio
    email
    name
    picture
    totalScore
    user_achievement {
      achievement {
        description
        name
        achievement_type
      }
    }
  }
}
