query ProfileUser($id: String!) {
  user(id: $id) {
    bio
    email
    name
    picture
    totalScore
    user_achievement(order_by: { created_at: asc }) {
      achievement {
        description
        name
        achievement_type
        level
        created_at
      }
    }
  }
}
