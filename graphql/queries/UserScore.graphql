query UserScore($id: String!, $category: String) {
  user(id: $id) {
    activities_aggregate(where: { geofence: { category: { _eq: $category } } }) {
      aggregate {
        sum {
          score
        }
      }
    }
  }
}
