query Highscore($timespan: timestamptz, $category: String) {
  users {
    id
    name
    picture
    activities_aggregate(where: { geofence: { category: { _eq: $category } } }, order_by: { score: desc_nulls_last }) {
      aggregate {
        sum {
          score
        }
      }
    }
  }
}
