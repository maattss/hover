mutation InsertFriendTracking($geofence_id: Int!, $linking_word: String!, $user_id: String!) {
  insert_friend_tracking(objects: { geofence_id: $geofence_id, linking_word: $linking_word, user_start_id: $user_id }) {
    returning {
      id
    }
  }
}
